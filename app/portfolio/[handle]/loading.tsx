import { FullPageLoader } from '@/components/brand/brand-loader'

/** A public portfolio, resolving. No layout chrome here, so the page waits whole. */
export default function PortfolioLoading() {
  return <FullPageLoader />
}
